#pragma once

#include <math.h>
#include <iostream>
#include <vector>

using namespace std;

vector<double> cos (const vector<double>);

vector<double> sin (const vector<double>);

vector<double> tan (const vector<double>);

vector<double> log (const vector<double>);

vector<double> log10(const vector<double>);

vector<double> exp(const vector<double>);

vector<double> pow(const vector<double>, const vector<double>);

vector<double> pow(const vector<double>, const double);

vector<double> pow(const double, const vector<double>);

vector<double> sqrt(const vector<double>);

vector<double> acos(const vector<double>);

vector<double> asin(const vector<double>);

vector<double> atan(const vector<double>);
